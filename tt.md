# 抖音合集数据爬取需求)

## 一、基础数据爬取（每个视频）

### 1. 视频基本信息

- [ ] 视频ID
- [ ] 视频标题
- [ ] 视频描述
- [ ] 视频URL
- [ ] 发布时间
- [ ] 视频时长

### 2. 视频互动数据

- [ ] 点赞量
- [ ] 收藏量
- [ ] 转发量/分享量
- [ ] 浏览量（如果接口提供）
- [ ] 评论总数

### 3. 评论数据（完整爬取）

- [ ] **一级评论**
  - 评论ID
  - 评论内容（文本）
  - 评论者昵称
  - 评论点赞量
  - 评论发布时间
  
- [ ] **二级评论（回复）**
  - 回复ID
  - 回复内容（文本）
  - 回复者昵称
  - 回复点赞量
  - 回复发布时间
  - 所属一级评论ID

## 二、数据分析需求

### 1. 关键词提取

- [ ] 提取所有**高赞评论**的关键词（如点赞数 > 平均值或前20%）
- [ ] 提取所有**视频标题+描述**的高频词/关键词
- [ ] 提取**高互动视频**（点赞/转发量高）的关键词

### 2. 评论分析

- [ ] 统计高赞视频对应的评论关键词
- [ ] 分析评论情感倾向（可选）
- [ ] 统计评论词频分布

### 3. 数据筛选条件

- 高赞评论定义：点赞数 > ____ （待定）
- 高互动视频定义：点赞数/转发数 > ____ （待定）

## 三、额外信息（需要确认）

- [ ] 视频相关话题标签
- [ ] 视频@的用户
- [ ] 作者信息（昵称、ID）

## 四、待确认需求

> ⚠️ 需要明确以下内容：
- **"相关政策"** 指的是什么？（视频内容相关的政策法规？）
- **"相关文件"** 指的是什么？（视频附件？还是其他？）

## 五、技术实现

### ✅ 可用的现有接口

1. **合集作品接口** (已实现)
   - 端点：`DouyinAPIEndpoints.MIX_AWEME`
   - 方法：`fetch_user_mix_videos(mix_id, cursor, count)`
   - 位置：`crawlers/douyin/web/web_crawler.py` 第165行
   - API路由：`/api/douyin/fetch_user_mix_videos`

2. **一级评论接口** (已实现)
   - 端点：`DouyinAPIEndpoints.POST_COMMENT`
   - 方法：`fetch_video_comments(aweme_id, cursor, count)`
   - 位置：`crawlers/douyin/web/web_crawler.py` 第225行
   - API路由：`/api/douyin/fetch_video_comments`

3. **二级评论接口** (已实现)
   - 端点：`DouyinAPIEndpoints.POST_COMMENT_REPLY`
   - 方法：`fetch_video_comments_reply(item_id, comment_id, cursor, count)`
   - 位置：`crawlers/douyin/web/web_crawler.py` 第237行
   - API路由：`/api/douyin/fetch_video_comment_replies`

### 📝 需要开发的功能

1. **数据聚合脚本**：整合上述接口，实现批量爬取
2. **关键词提取**：使用jieba或其他分词工具提取关键词（可以使用更高级的内容）
3. **数据存储**：将数据保存到CS文件
4. **数据分析**：统计高频词、高赞内容等完整的数据分析，（暂时不用实现）
